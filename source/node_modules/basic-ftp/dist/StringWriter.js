"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
var stream_1 = require("stream");
var StringWriter = /** @class */ (function (_super) {
    __extends(StringWriter, _super);
    function StringWriter() {
        var _this = _super.call(this) || this;
        _this.buf = Buffer.alloc(0);
        _this._write = function (chunk, _, done) {
            if (chunk) {
                if (chunk instanceof Buffer) {
                    _this.buf = Buffer.concat([_this.buf, chunk]);
                }
                else {
                    done(new Error("StringWriter expects chunks of type 'Buffer'."));
                    return;
                }
            }
            done();
        };
        return _this;
    }
    StringWriter.prototype.getText = function (encoding) {
        return this.buf.toString(encoding);
    };
    return StringWriter;
}(stream_1.Writable));
exports.StringWriter = StringWriter;
